# Variables d'environnement pour le développement local
# Configuration Supabase
VITE_SUPABASE_URL=votre_url_supabase
VITE_SUPABASE_ANON_KEY=votre_anon_key

# Clé de service Supabase (avec privilèges admin) - NE JAMAIS EXPOSER CÔTÉ CLIENT
# Trouvez-la dans: Supabase Dashboard > Settings > API > service_role key (secret)
# Cette clé permet la création automatique du bucket et des policies RLS
VITE_SUPABASE_SERVICE_KEY=votre_service_role_key

# Environnement de développement
NODE_ENV=development

# Configuration API locale (si vous avez une API backend)
VITE_API_URL=http://localhost:3003

# Configuration optionnelle pour la production (laisser vide pour le dev)
TEMPLATE_BANNER_SCRIPT_URL=
TEMPLATE_REDIRECT_URL=

# ⚠️ ATTENTION : Les variables VITE_RESEND_* ne sont plus utilisées
# L'envoi d'emails se fait maintenant via SMTP Gmail gratuit dans l'Edge Function
# Configuration à faire dans Supabase Edge Functions :
# supabase secrets set SMTP_USERNAME=votre.email@gmail.com
# supabase secrets set SMTP_PASSWORD=votre_app_password_gmail
# Voir QUICK_START_SMTP_GRATUIT.md pour plus d'infos
